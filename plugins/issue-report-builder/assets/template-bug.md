# 【Bug 反馈单】<一句话概括这批问题，如：应用管理页编辑/创建相关问题 3 则>

> 填写者：<填写人>　|　日期：<YYYY-MM-DD>　|　共 <N> 个问题

## 上下文环境

以下环境信息适用于本单全部问题（个别问题若环境不同，在该问题内单独注明）：

| 项 | 内容 |
|---|---|
| 环境 | <生产 / 测试 / 预发> |
| 系统版本 | <如 v2.3.1，不清楚可写"未知"> |
| 浏览器 | <如 Chrome 125> |
| 操作账号/角色 | <用什么账号、什么角色操作的> |

---

## 问题 1：<一句话概括，让研发一眼知道哪坏了>

> 优先级：<高/中/低，按影响范围和严重度判断>

### 问题现象

<客观描述"发生了什么"和"本该发生什么"。例：编辑应用时 OAuth2 配置项未回显，应带出已保存的值。>

### 所属页面/模块

<页面地址或模块名。跨页面时每个问题各自注明。>

### 复现步骤

照着就能重现，每步写清"操作 + 输入的参数/数据"：

1. <进入 XX 页面>
2. <在 XX 输入框填入：`具体参数值`>
3. <点击 XX 按钮>
4. <出现问题：……>

**复现频率**：<必现 / 偶发（约 X 次出现 1 次）>

### 截图

<出问题那一刻的图，放 ./assets/ 下，文件名自解释。若暂无截图，写"待补充：请在出问题时截图存入 ./assets/"。>

![问题1现象](./assets/<截图文件名>.png)

### 浏览器控制台错误

<F12 → Console 的红色报错，保留堆栈。若控制台无报错，明确写"无报错"，不要留空。>

```
<粘贴控制台报错原文；无则写：无报错>
```

**失败接口**：<若有，填 `<方法> <URL>` → 状态码；请求参数；响应体。无则写"不涉及"。>

### 后端日志

<相关时间段的后端日志，由用户从日志系统/服务器捞好后粘贴，已截取并标注关键报错行。
若本问题不需要后端日志（如纯前端问题），写"不涉及"并简述原因。>

```
<粘贴后端日志；不需要则删除此代码块，仅保留上面的"不涉及"说明>
```

### 关键数据标识

<订单号 / 用户ID / 数据ID 等，研发拿它去查库。无则写"不涉及"。>

### 补充说明

<是否最近才出现、是否只在特定数据上出现等。若有方向性的原因猜测，可简述并标注"供研发参考"——点到为止，定位根因是研发的职责。无则写"无"。>

---

## 问题 2：<一句话概括>

> 优先级：<高/中/低>

### 问题现象

<...>

### 所属页面/模块

<...>

### 复现步骤

1. <...>

**复现频率**：<...>

### 截图

![问题2现象](./assets/<截图文件名>.png)

### 浏览器控制台错误

```
<...；无则写：无报错>
```

**失败接口**：<...；无则写"不涉及">

### 后端日志

<...；不需要则写"不涉及"并简述原因>

```
<粘贴后端日志；不需要则删除此代码块>
```

### 关键数据标识

<...；无则写"不涉及">

### 补充说明

<...；无则写"无">

---

> 按上面的小节格式继续追加 问题 3、问题 4……每个问题之间用 `---` 分隔。
